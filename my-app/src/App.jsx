import ChessBoard from "./components/ChessBoard";

function App() {
  return (
      <div>
        <header>
          <h1>Chess.net (no relation)</h1>
        </header>

        <ChessBoard/>
      </div>
    );
}

export default App;
